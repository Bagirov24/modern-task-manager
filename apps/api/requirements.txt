# =============================================================================
# API dependencies
# Pin to exact versions for reproducible builds.
# Run `pip-audit` in CI to catch known CVEs in these packages.
# =============================================================================

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.27.0

# --- Database ---
sqlalchemy==2.0.25
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9

# --- Validation ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
email-validator==2.2.0

# --- Auth ---
PyJWT[crypto]==2.13.0
passlib[bcrypt]==1.7.4

# --- Cache / message broker ---
redis==5.0.1
celery==5.3.6

# --- WebSocket ---
python-socketio==5.16.3

# --- HTTP client (integrations + tests) ---
httpx==0.26.0

# --- Structured logging ---
structlog==24.1.0

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==5.0.0
anyio[trio]==4.2.0
